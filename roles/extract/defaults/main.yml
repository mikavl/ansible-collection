---

# Patterns to extract from the archive
extract_patterns: "*"

# Subpath to find files inside the archive
extract_subpath: ""

# File mode and ownership options
extract_owner: root
extract_group: root
extract_mode: "0644"
